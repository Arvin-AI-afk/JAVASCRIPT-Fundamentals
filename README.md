# How to install Git

# 1. Update your package list to ensure you get the latest version
sudo apt update

# 2. Install Git
sudo apt install git -y

# 3. Verify the installation
git --version

# 4. Config your Git Identity
git config user.name "Arvin O. Paguio"
git config user.email "apaguio.ai@gmail.com"